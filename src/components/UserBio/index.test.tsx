/**
 * test that by default the octocat data is displyed
 * test that what ever data is passed is what is displayed
 * test that if data is empty the default octocatUserData is shown.
 * 
 */
export {};